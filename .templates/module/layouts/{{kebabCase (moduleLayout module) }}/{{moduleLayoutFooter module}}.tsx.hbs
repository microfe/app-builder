import React from 'react';
import styled from 'styled-components';

const _{{ moduleLayoutFooter module }} = styled.div`
`;

interface I{{ moduleLayoutFooter module }}Props {

}

export const {{ moduleLayoutFooter module }} = (props: React.PropsWithChildren<I{{ moduleLayoutFooter module }}Props>) => {
  const { } = props;

  return (
    <_{{ moduleLayoutFooter module }} id="{{ moduleLayoutFooter module }}">
        {props.children}
    </_{{ moduleLayoutFooter module }}>
  );
};