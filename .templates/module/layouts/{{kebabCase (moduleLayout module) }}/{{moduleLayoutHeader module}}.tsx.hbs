import React from 'react';
import styled from 'styled-components';

const _{{ moduleLayoutHeader module }} = styled.div`
`;

interface I{{ moduleLayoutHeader module }}Props {

}

export const {{ moduleLayoutHeader module }} = (props: React.PropsWithChildren<I{{ moduleLayoutHeader module }}Props>) => {
  const { } = props;

  return (
    <_{{ moduleLayoutHeader module }} id="{{ moduleLayoutHeader module }}">
        {props.children}
    </_{{ moduleLayoutHeader module }}>
  );
};