import React from 'react';
import styled from 'styled-components';

const _{{ moduleLayoutBody module }} = styled.div`
`;

interface I{{ moduleLayoutBody module }}Props {

}

export const {{ moduleLayoutBody module }} = (props: React.PropsWithChildren<I{{ moduleLayoutBody module }}Props>) => {
  const { } = props;

  return (
    <_{{ moduleLayoutBody module }} id="{{ moduleLayoutBody module }}">
        {props.children}
    </_{{ moduleLayoutBody module }}>
  );
};