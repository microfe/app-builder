import {
IApplicationModule,
IApplicationModuleMountProps
} from 'libs/framework';
import React from 'react';

import {
  {{ moduleContext module}},
  {{ modulePath module }},
  {{ moduleContextInterface module }}
} from './configs';
import { pages } from './pages';
import { {{ moduleLayout module }} } from './layouts';

const MountPoint = (props: IApplicationModuleMountProps) => {
const { children } = props;

const contextValue: {{ moduleContextInterface module }} = { helloText: 'Hello World!'};
  return (
    <{{ moduleContext module}}.Provider value={contextValue}>
      <{{ moduleLayout module }}>
        {children()}
      </{{ moduleLayout module }}>
    </{{ moduleContext module}}.Provider>
  );
}; 

const module: IApplicationModule = {
  title: '{{ titleCase module }}',
  path: {{ modulePath module }},
  defaultUrl: '/{{ kebabCase module }}',
  pages: pages,
  mount: MountPoint,
  translation: {},
  permissions: {}
};

export default module;